{
  "name": "liza",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch host/main.ts",
    "host": "tsx host/main.ts",
    "test": "node --import tsx --test host/**/*.test.ts",
    "check": "npm run build && npm test",
    "build:dos": "powershell -NoProfile -ExecutionPolicy Bypass -File tools/build_dos.ps1"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.7",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@sinclair/typebox": "^0.34.52",
    "serialport": "^13.0.0",
    "undici-types": "^8.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
